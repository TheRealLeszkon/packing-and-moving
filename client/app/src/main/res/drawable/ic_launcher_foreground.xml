<?xml version="1.0" encoding="utf-8"?>
<!-- The box artwork centered and inset into the adaptive-icon safe zone so the launcher
     mask never clips it. Pairs with a white background (the artwork's own background). -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_launcher_image"
    android:inset="18%" />
