<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The FastAPI backend runs over plain HTTP during development (10.0.2.2 = host
         machine from the emulator, or a LAN IP for a physical device). Restricting
         cleartext to these hosts avoids weakening security for any other traffic. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
