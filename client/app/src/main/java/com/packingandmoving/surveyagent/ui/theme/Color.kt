package com.packingandmoving.surveyagent.ui.theme

import androidx.compose.ui.graphics.Color

// Kinetic Material palette (DESIGN.md §1). High-energy brand colors stay vibrant in
// both light and dark schemes; only the neutral surfaces invert.

val Primary = Color(0xFFFF4500)   // High-energy orange — main actions
val Secondary = Color(0xFFFF8C00) // Amber — secondary actions / warnings
val Tertiary = Color(0xFF00BCD4)  // Cyan — info / success badges

// Light neutrals
val Surface = Color(0xFFF9F9FF)
val SurfaceContainer = Color(0xFFEEF1FF)
val OnSurface = Color(0xFF191B23)
val OnSurfaceVariant = Color(0xFF43474E)

// Dark neutrals
val SurfaceDark = Color(0xFF121212)
val SurfaceContainerDark = Color(0xFF1E1E1E)
val OnSurfaceDark = Color(0xFFE3E2E9)
val OnSurfaceVariantDark = Color(0xFFC4C6D0)
